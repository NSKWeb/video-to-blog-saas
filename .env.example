# ============================================
# Video-to-Blog SaaS Environment Configuration
# ============================================

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://user:password@localhost:5432/video_to_blog?schema=public"

# --------------------------------------------
# OpenAI Configuration
# --------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"

# OpenAI model to use for blog generation
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL="gpt-4o-mini"

# OpenAI API timeout in milliseconds (default: 60000)
OPENAI_TIMEOUT="60000"

# Maximum number of retries for OpenAI API calls (default: 3)
OPENAI_MAX_RETRIES="3"

# --------------------------------------------
# Deepgram Configuration
# --------------------------------------------
# Get your API key from: https://console.deepgram.com/
DEEPGRAM_API_KEY="your-deepgram-api-key-here"

# Deepgram model to use for transcription
# Options: nova-2, nova, base, enhance
DEEPGRAM_MODEL="nova-2"

# Default language for transcription (default: en-US)
DEEPGRAM_LANGUAGE="en-US"

# Deepgram API timeout in milliseconds (default: 30000)
DEEPGRAM_TIMEOUT="30000"

# --------------------------------------------
# WordPress Configuration (Optional for MVP)
# --------------------------------------------
# Your WordPress site URL
WORDPRESS_SITE_URL="https://your-wordpress-site.com"

# WordPress username (for Application Password authentication)
WORDPRESS_USERNAME="your-username-here"

# WordPress Application Password
# Generate from: WordPress Admin > Users > Profile > Application Passwords
WORDPRESS_APP_PASSWORD="your-wordpress-app-password-here"

# --------------------------------------------
# Application Configuration
# --------------------------------------------
# Node Environment: development, production, test
NODE_ENV="development"

# Log level: error, warn, info, debug (default: info)
LOG_LEVEL="info"

# API timeout in milliseconds (default: 30000)
API_TIMEOUT="30000"

# Maximum video file size (e.g., 500MB, 1GB, 2GB)
MAX_VIDEO_SIZE="500MB"

# --------------------------------------------
# Additional Notes
# --------------------------------------------
# 1. Make sure to copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to version control
# 3. WordPress configuration is optional for MVP - can be added later
# 4. For production, use environment variable management (e.g., Vercel, AWS, etc.)